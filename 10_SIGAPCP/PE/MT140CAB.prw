#Include "Protheus.ch"

User Function MT140CAB()
Local lRet := .T.

    //If
    //	Regra existente
    //	[...]
    //EndIf

    // Ponto de chamada ConexãoNF-e sempre como última instrução.
    If lRet
        //lRet := U_GTPE009()	 
    EndIf
    
Return lRet
